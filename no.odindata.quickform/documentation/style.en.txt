no.odindata.quickform style elements
====================================

<(show-form)> is the element displaying the form. By default the form is in the XHTML form format generated by datamanager.

<(show-form-finished)> is the message shown to user after the form has been sent, and <(show-form-failed)> is the message shown in form sending fails for some reason.